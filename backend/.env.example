# =================================================================
# 播客生成系統 - 環境變數配置範例
# =================================================================
# 複製此檔案為 .env 並填入實際值
# =================================================================

# =================================================================
# Phase 1: 路徑配置（CLI/Server 拆分支援）
# =================================================================
# 這些環境變數允許你覆寫預設路徑，支援 CLI 與 API 服務的獨立部署
#
# OUTPUT_ROOT - 輸出目錄（生成的音訊、字幕等）
# 預設值：../output（與 CLI 共享）
# OUTPUT_ROOT=../output
#
# DATA_ROOT - 數據目錄（books、transcripts 等）
# 預設值：../output（若需書籍原始文本，可指向 ../data）
# DATA_ROOT=../output
#
# CONFIG_ROOT - 配置檔案目錄（podcast_config.yaml 所在位置）
# 預設值：./config
# CONFIG_ROOT=/path/to/custom/config

# =================================================================
# API Keys（必需）
# =================================================================

# Gemini API Key (必需)
# 用於：LLM 腳本生成 + Multi-Speaker TTS
GEMINI_API_KEY=your_gemini_api_key_here

# =================================================================
# Google Cloud Storage 認證（選填，用於資料同步）
# =================================================================

# GCS 服務帳號認證檔案路徑
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# GCS 同步目標 bucket
STORYTELLING_SYNC_BUCKET=gs://your-bucket-name/output

# CLI / Worker 同步排除規則（正則表達式）
# 預設排除 .DS_Store、.gitignore、.env、.pytest_cache、.wav 與 .textgrid 檔案
STORYTELLING_SYNC_EXCLUDE_REGEX="(^|/)\\.DS_Store$|(^|/)\\.gitignore$|(^|/)\\.env$|(^|/)\\.pytest_cache($|/.*)|.*\\.wav$|.*\\.textgrid$"

# =================================================================
# 批次處理配置（選填）
# =================================================================

# 腳本生成批次大小與延遲
PODCAST_SCRIPT_BATCH_SIZE=10
PODCAST_SCRIPT_BATCH_DELAY=10

# 音訊生成批次大小與延遲
PODCAST_AUDIO_BATCH_SIZE=3
PODCAST_AUDIO_BATCH_DELAY=60

# =================================================================
# 新聞功能配置（選填）
# =================================================================

# 啟用新聞功能（0=關閉, 1=啟用）
NEWS_FEATURE_ENABLED=0

# NewsData.io API Key（啟用新聞功能時必填）
NEWSDATA_API_KEY=your_newsdata_api_key_here

# 新聞預設語言代碼
NEWSDATA_DEFAULT_LANGUAGE=en

# 新聞預設國家代碼（選填）
NEWSDATA_DEFAULT_COUNTRY=us

# 分類白名單（逗號分隔，留空 = 允許所有分類）
NEWS_CATEGORY_WHITELIST=

# 新聞預設數量
NEWS_DEFAULT_COUNT=10

# 新聞最大數量
NEWS_MAX_COUNT=10

# =================================================================
# FastAPI 伺服器配置（選填）
# =================================================================

# CORS 允許的來源（逗號分隔）
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com

# Gzip 壓縮最小大小（bytes）
# GZIP_MIN_SIZE=512

# Sentence Explainer 模型
# SENTENCE_EXPLAINER_MODEL=gemini-2.5-flash-lite

# Sentence Explainer 超時時間（秒）
# SENTENCE_EXPLAINER_TIMEOUT=30

# Sentence Explainer 快取大小
# SENTENCE_EXPLAINER_CACHE_SIZE=128

# 媒體傳遞模式（local 或 gcs）
# MEDIA_DELIVERY_MODE=local

# GCS Mirror 包含的檔案後綴（逗號分隔）
# GCS_MIRROR_INCLUDE_SUFFIXES=.mp3,.srt,.json

# Signed URL TTL（秒）
# SIGNED_URL_TTL_SECONDS=600

# 新聞快取 TTL（秒）
# NEWS_CACHE_TTL_SECONDS=900

# 新聞 HTTP 超時時間（秒）
# NEWS_HTTP_TIMEOUT=10
